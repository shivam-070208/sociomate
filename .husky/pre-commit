set -e
cd "$(git rev-parse --show-toplevel)"

echo "Running lint-staged (linting and formatting)..."
bunx lint-staged

echo "Running type check (tsc --noEmit) from repository root..."
bun run check-types

echo "Running test suite..."
bun run test

